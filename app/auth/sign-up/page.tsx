'use client'

import { FormEvent, useState } from 'react'
import { createClient } from '@/lib/supabase/client'

export default function SignUpPage() { const [name, setName] = useState(''); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [message, setMessage] = useState(''); const [loading, setLoading] = useState(false); async function submit(event: FormEvent) { event.preventDefault(); setLoading(true); setMessage(''); const { error } = await createClient().auth.signUp({ email, password, options: { emailRedirectTo: process.env.NEXT_PUBLIC_DEV_SUPABASE_REDIRECT_URL || `${window.location.origin}/auth/callback`, data: { full_name: name } } }); setLoading(false); setMessage(error ? (error.message.toLowerCase().includes('password') ? 'পাসওয়ার্ড আরও শক্তিশালী দিন' : 'সাইন আপ করা যায়নি, তথ্য যাচাই করুন') : 'ইমেইল কনফার্ম করার জন্য আপনার ইনবক্স দেখুন।'); } return <main className="grid min-h-screen place-items-center bg-slate-50 p-5"><section className="w-full max-w-md rounded-3xl border bg-white p-7 shadow-xl"><a href="/" className="inline-flex h-12 w-[186px] items-center" aria-label="EasySkillBD হোম"><img src="https://hebbkx1anhila5yf.public.blob.vercel-storage.com/155x40-ICX8Yasl9AeWQHpBCtRD62Ll403Xlf.png" alt="EasySkillBD logo" width={155} height={40} className="h-10 w-[155px] object-contain object-left" /></a><h1 className="mt-8 text-3xl font-extrabold">রেজিস্টার করুন</h1><p className="mt-2 text-sm text-slate-500">ডিফল্টভাবে নতুন অ্যাকাউন্ট Student রোল পাবে।</p><form onSubmit={submit} className="mt-7 flex flex-col gap-4"><label className="text-sm font-bold">পুরো নাম<input required value={name} onChange={(e) => setName(e.target.value)} className="mt-2 w-full rounded-xl border p-3" /></label><label className="text-sm font-bold">ইমেইল<input required type="email" value={email} onChange={(e) => setEmail(e.target.value)} className="mt-2 w-full rounded-xl border p-3" /></label><label className="text-sm font-bold">পাসওয়ার্ড<input required minLength={8} type="password" value={password} onChange={(e) => setPassword(e.target.value)} className="mt-2 w-full rounded-xl border p-3" /></label>{message && <p className="text-sm font-semibold text-emerald-700">{message}</p>}<button disabled={loading} className="rounded-xl bg-emerald-600 py-3 font-bold text-white">{loading ? 'অপেক্ষা করুন...' : 'অ্যাকাউন্ট তৈরি করুন'}</button><a href="/auth/login" className="text-center text-sm font-semibold text-emerald-700">আগের অ্যাকাউন্টে লগইন করুন</a></form></section></main> }
